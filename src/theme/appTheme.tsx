import { StyleSheet } from 'react-native';

export const appTheme = StyleSheet.create({
    appContainer: {
        marginHorizontal: 20,
    },
    title: {
        fontSize: 35,
        fontWeight: 'bold',
    },
});
